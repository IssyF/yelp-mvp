<div class="header">
  <h1>Add a Review for <%= @restaurant.name %></h1>
</div>

<div class="content">
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.select :rating, (0..5).to_a, prompt: 'Select Rating' %>
    <%= f.submit %>
  <% end %>
  <br>
  <p><%= link_to "👈 Go back", restaurant_path(@restaurant), class: "back" %></p>
  <p><%= link_to "👈 Go back to all restaurants", restaurants_path, class: "back" %></p>
</div>
