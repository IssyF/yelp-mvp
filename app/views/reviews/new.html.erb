<h1>Add a Review for <%= @restaurant.name %></h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.select :rating, (0..5).to_a, prompt: 'Select Rating' %>
  <%= f.submit %>
<% end %>

<p><%= link_to "Go back", restaurant_path(@restaurant) %></p>
